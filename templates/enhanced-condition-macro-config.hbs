<form data-condition-id="{{condition.id}}">
    <h2>{{condition.name}} - {{localize "CLT.ENHANCED_CONDITIONS.MacroConfig.Heading"}}</h3>
    <p class="notes">{{localize "CLT.ENHANCED_CONDITIONS.MacroConfig.Description"}}</p>
    <div class="flexrow">
        <div class="form-group">
            <label for="macro-select-apply">{{localize "CLT.ENHANCED_CONDITIONS.MacroConfig.ApplyHeading"}}</label>
            <select name="macro-select-apply" class="macro-select">
                <option value="">{{localize "CLT.ENHANCED_CONDITIONS.MacroConfig.SelectNone"}}</option>
                {{selectOptions macroChoices selected=applyMacroId nameAttr="id" labelAttr="name" localize=true}}
            </select>
        </div>
    </div>
    <hr/>
    <div class="flexrow">
        <div class="form-group">
            <label for="macro-select-remove">{{localize "CLT.ENHANCED_CONDITIONS.MacroConfig.RemoveHeading"}}</label>
            <select name="macro-select-remove" class="macro-select">
                <option value="">{{localize "CLT.ENHANCED_CONDITIONS.MacroConfig.SelectNone"}}</option>
                {{selectOptions macroChoices selected=removeMacroId nameAttr="id" labelAttr="name" localize=true}}
            </select>
        </div>
    </div>
    <br/>
    <div class="footer">
        {{canSave}}
        <button name="save" class="save" type="submit">
            <i class="fas fa-save"></i>&nbsp;{{localize "Save"}}
        </button>
    </div>
</form>